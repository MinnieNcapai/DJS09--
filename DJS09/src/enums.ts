// Enum for user permissions
// Defines different access levels for users
export enum Permissions {
    ADMIN = 'ADMIN',  // Admin users with full access
    READ_ONLY = 'READ_ONLY' // Users with read-only access
}

// Enum for loyalty user levels
// Defines different loyalty levels for users
export enum LoyaltyUser {
    GOLD_USER = 'GOLD_USER', // Highest loyalty level
    SILVER_USER = 'SILVER_USER',  // Mid-tier loyalty level
    BRONZE_USER = 'BRONZE_USER' // Lowest loyalty level
}